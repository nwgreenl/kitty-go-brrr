export const BASE_URL = 'https://placekitten.com/';
export const DEFAULT_DIMS = {
  width: 200,
  height: 300,
};
export const DEFAULT_URL = `${BASE_URL}/${DEFAULT_DIMS.width}/${DEFAULT_DIMS.height}`;
export const DEFAULT_ASPECT_STATE = true;
